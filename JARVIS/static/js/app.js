define([
    'jquery',
    'underscore',
    'backbone',
], function($, _, Backbone) {

    var initialize = function() {
        console.log("STILL WORKS!");
    };

    return {
        initialize: initialize
    };
});
